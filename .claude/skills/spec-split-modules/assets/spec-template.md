# M{n} {模块名}模块规格文档

**父规格**：`{parent-spec-path}`
**模块编号**：M{n}
**依赖**：{列出依赖模块，例：M1（types + api + store 基座）；若无依赖写"无"}
**被依赖**：{列出被依赖模块；若为叶子模块可写"无"}

---

## 1. 模块范围

{一句话概括本模块解决什么问题，交付后带来什么用户价值}

### 1.1 交付物

{列出具体文件/组件/接口，逐项列清楚；避免"若干工具方法"这种模糊描述}

- 类型/接口：`path/to/file.ts`（N 个接口）
- API 层：`path/to/api.ts`（N 个函数）
- 组件/页面：`path/to/Component.tsx`
- 状态管理：`store/xxx.ts`
- 路由/菜单：修改 `router/index.tsx`

### 1.2 非目标

{显式列出本模块不做什么。指出交给哪个模块处理}

- 不包含 X（属 M{k}）
- 不包含 Y（属 M{k}）

---

## 2. {核心设计段落} 例：组件设计 / 接口设计 / 状态模型

{按父规格切片粘出对应章节，或概括后引用父规格章节号}

### 2.1 xxx

**Props / Signature**：
```typescript
// 代码骨架，不必全量粘贴
```

**详见父规格 §{章节号}**

---

## 3. 数据流 / 关键实现细节

{描述模块内的数据流动、边界处理、竞态等}

```
用户操作 → 组件 → store → api → 后端
       ← 更新 UI ←  ←     ←
```

---

## 4. 关键决策（继承父规格）

| 决策点 | 选择 | 依据（父规格章节） |
|---|---|---|
| xx | yy | §{n} |

---

## 5. 验证边界

**功能验证**：见 `{对应 e2e 文档路径}`
**技术验证**：TS 编译、组件 render、API 联通

---

## 6. 交付验收

- [ ] 上述交付物文件已创建
- [ ] E2E 文档所有检查项通过
- [ ] 依赖 M{k} 且不阻塞 M{k+n}

---

**文档版本**：v1.0  |  **拆分自**：父规格 §{对应章节}
